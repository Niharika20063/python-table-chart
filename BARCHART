import matplotlib.pyplot as plt
import numpy as np
over=np.arange(1,11)
score=[9,2,4,7,8,13,6,7,3,5]
plt.bar(overs,scores)
plt.title("INDIA SCORE OVER WISE")
plt.xlabel("OVERS FROM 1 TO 10")
plt.ylabel("RUNS BASED ON OVERS ")
plt.xticks(overs)
plt.grid(axis='y',linestyle="--")
plt.show()
